package sina;

import javax.xml.soap.Node;

public class Main1 {

	/*
	void AddLeftToRight(Node root) {
		Node head = new Node(0);
		Node pre = head;
        while (root != null) {
            if (root.left != null) {
                pre.next = root.left;
                pre = pre.next;
            }
            if (root.right != null) {
                pre.next = root.right;
                pre = pre.next;
            }
            root = root.next;
            if (root == null) {
                pre = head;
                root = head.next;
                head.next = null;
            }
        }
	}
	*/
	
	public static void main(String[] args) {
		// TODO Auto-generated method stub

	}

}
